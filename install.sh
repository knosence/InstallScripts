
#!/usr/bin/env bash

  #dox install / GitRepos
  function gitrepos(){
  }
  #dox install / dox
  read dox
  
  }
  #dox install / fresh
  function fresh(){
    git status
  }
  #dox install / server
  function server(){
    git status
  }
  #dox install / dev-env 
  function dev(){
    git status
  }
